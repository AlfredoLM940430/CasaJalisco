import { BrowserRouter } from "react-router-dom"
import { AppRouer } from "./router/AppRouer"

export const LottoApp = () => {
    return (
        <BrowserRouter>
            <AppRouer/>
        </BrowserRouter>
)}